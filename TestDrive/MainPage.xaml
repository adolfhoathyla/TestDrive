<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:TestDrive" x:Class="TestDrive.MainPage" Padding="3, 20, 3, 3">
    <ListView x:Name="listViewVeiculos" ItemsSource="{Binding Veiculos}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Nome}"></Label>
                            <Label Text=" - "></Label>
                            <Label Text="{Binding Preco}" FontAttributes="Bold"></Label>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>